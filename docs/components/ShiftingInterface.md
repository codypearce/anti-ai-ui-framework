# ShiftingInterface

Login-like UI where elements move, duplicate, and change colors.

Props
- `shiftInterval?: number`
- `duplicateChance?: number`
- `colorChangeInterval?: number`

Usage
```tsx
<ShiftingInterface shiftInterval={1200} duplicateChance={0.25} />
```

